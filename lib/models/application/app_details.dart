
class AppDetails {
  final String appName, packageName, buildNumber, version;
  AppDetails(
      {required this.appName,
      required this.packageName,
      required this.buildNumber,
      required this.version});
}
